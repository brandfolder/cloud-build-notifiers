steps:
# todo
#- id: gokart

  - name: securego/gosec
    args:  ['-severity', 'medium', '-quiet', './...']

  - id: gofmt
    name: golang
    entrypoint: 'bash'
    args:
     - -c 
     - |
       test -z $(gofmt -s -l .)
